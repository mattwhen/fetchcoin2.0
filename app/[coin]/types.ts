export interface CoinDetails {
    id: string,
    icon: string,
    name: string,
    symbol: string,
    rank: number,
    price: number,
    volume: number,
    marketCap: number, 
    availableSupply: number
}